# protocol

Pure protocol definitions only.

Owns:
- UUID/constants that have an evidence source,
- frame encoding/decoding,
- CRC,
- response parsing,
- command allow-list,
- protocol tests.

Must not depend on Android UI, storage or HTTP.
